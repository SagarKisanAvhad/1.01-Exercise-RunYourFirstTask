/*
To tell Gradle to use this practice file as the build script instead of the
default build.gradle, use:

./gradlew -b practice.gradle hello
*/

task hello {
  description "First Gradle Project"
  group "demo"
  doLast {
    println "Sagar is soon to become Awesome gradle developer"
  }
}